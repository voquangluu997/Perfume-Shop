export enum RATING {
  'one' = 1,
  'two' = 2,
  'three' = 3,
  'four' = 4,
  'five' = 5,
}

export enum CART_STATUS {
  ORDERING = 'ORDERING',
  NON_ORDER = 'NON_ORDER',
  ORDERED = 'ORDERED',
}

export enum BOOKING_STATUS {
  ORDERING = 'ORDERING',
  NON_ORDER = 'NON_ORDER',
  ORDERED = 'ORDERED',
}

export enum PAYMENT_METHOD {
  CASH = 'CASH',
  DEBIT = 'DEBIT',
}
